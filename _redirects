# Cloudflare Pages redirects

# API routes should go to the Functions first
/api/*       /api/:splat       200

# Specific page routes (must come before catch-all)
/login       /login.html       200
/dashboard   /dashboard.html   200

# Fallback for all other routes (must be last)
/*           /index.html       200